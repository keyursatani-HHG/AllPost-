# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database (async) ---
SQLAlchemy==2.0.36
asyncpg==0.30.0          # PostgreSQL (production)
aiosqlite==0.20.0        # SQLite (local dev / tests)
alembic==1.14.0

# --- Auth & security ---
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# --- Cache / rate limiting ---
redis==5.2.1

# --- Storage (media uploads) ---
boto3==1.35.90

# --- HTTP client ---
httpx==0.28.1
